import styled from 'styled-components';

import theme from '@/theme';

export const CitySearchInput = styled.input`
  max-width: ${theme.widths[10]}px;
  font-size: ${theme.fontSizes[4]}px;
  text-align: center;
  align-self: center;
  background-color: transparent;
  color: white;
  border: none;
  outline: none;
`;
